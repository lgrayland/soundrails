<div class="small-6 small-centered columns">
<%= form_for @track do |f| %>
<%= render partial: 'track_form', locals: { f: f } %>
<%= f.submit 'Add Track!', class: "tiny button" %>
</div>
<% end %>
</div>